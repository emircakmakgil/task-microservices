spring:
      application:
          name: "user-service"
      datasource:
        url: "jdbc:postgresql://localhost:5432/user-service"
        username: postgres
        password: test
      jpa:
        properties:
          hibernate:
            dialect: org.hibernate.dialect.PostgreSQLDialect
        showSql: true
        hibernate:
          ddlAuto: update
server:
  port: 8080

